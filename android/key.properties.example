# Copy this file to android/key.properties and keep the copy out of source control.
# Paths are resolved relative to the android directory.
storeFile=release/upload-keystore.jks
storePassword=REPLACE_WITH_KEYSTORE_PASSWORD
keyAlias=REPLACE_WITH_KEY_ALIAS
keyPassword=REPLACE_WITH_KEY_PASSWORD